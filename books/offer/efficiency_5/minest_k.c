/*subject: Find the minest k items from one array
 * solve 1:first sort the array by asc,then input the first k item.it cost O(nlogn)
 * solve 2:set one small container which has k locats,then from the array,one by one putted into the container,if container's items<k,exec insert,or else replace the max by the item from array.
 * solve 3:
 */



